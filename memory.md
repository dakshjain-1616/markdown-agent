# Memory

This file is managed by the Markdown Agent.
Edit the **Context** section to give the agent persistent background knowledge.

## Context
Agent is operating in Python best-practices mode.
Prefer modern tooling (uv, ruff, pyright) over legacy alternatives (pip, flake8, mypy).

## State
- last_run: never
- total_sessions: 0
- status: initialised

## Sessions

